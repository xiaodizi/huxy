import SwiftUI

enum TerminalTheme {
    static let bgTop = Color(red: 0.05, green: 0.05, blue: 0.07)
    static let bgBottom = Color(red: 0.08, green: 0.06, blue: 0.10)
    static let panelBg = Color(.sRGB, red: 0.04, green: 0.04, blue: 0.05, opacity: 0.45)
    static let panelInner = Color(.sRGB, red: 0.06, green: 0.06, blue: 0.08, opacity: 0.6)
    static let orange = Color(.sRGB, red: 1.0, green: 0.55, blue: 0.2)
    static let muted = Color(.sRGB, red: 0.7, green: 0.74, blue: 0.78)
    static let accentGreen = Color(.sRGB, red: 0.2, green: 0.95, blue: 0.8)
}
